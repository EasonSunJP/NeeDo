# Manual eKYC Backend Requirements
Repository: /Users/eason/Documents/New project/.worktrees/merchant-main-runtime. Base b8957b06.
Read README, AGENTS, docs/00_MASTER_MICRO_STEP_PLAN.md, docs/12_BACKOFFICE_MERCHANT_ADMIN_REAL_DATA.md and docs/User Management.md as required. User explicitly approved operations manual eKYC and adding pages. You own backend ONLY; parent owns frontend/nav/browser/PDF. Do not touch frontend or root checkout. Do not migrate local database, seed accounts, or restart services; parent handles runtime. Do not commit yet (shared checkout). TDD tests first.

Persist real eKycApplication (or similarly named) with encrypted complete submitted profile, user relation/index, status submitted/approved/rejected/withdrawn, version, timestamps, reviewer user FK/index, rejectionReason/reviewNote, deletedAt. Serialize concurrent duplicate submissions using user row/transaction or unique active constraint; closed/rejected supports new submission, immutable submitted snapshots. Never create application on GET/page entry.
Profile exact fields strings: familyName,givenName,familyNameKana,givenNameKana,birthYear,birthMonth,birthDay,sex ('male'/'female'),postalCode,city,street,building,occupation,otherOccupation. Reuse validation semantics from frontend ekycProfileModel (copy boundary into backend, no cross-build dependency); bound lengths, valid nonfuture actual calendar date, katakana normalize NFKC, postal7digits, occupations employee/executive/civil_servant/self_employed/part_time/contract/homemaker/student/retired/other and other required when chosen. Excluded fields must not be added. All PII profile encrypted with SensitiveFieldCipherService, audits identifiers/action/status only.
API contract (tell parent immediately if change needed):
GET /api/v1/ekyc-applications/mine?page&page_size -> standard paginated latest-first summaries {id,userId,status,version,createdAt,updatedAt,reviewedAt,reviewNote,rejectionReason}.
GET /api/v1/ekyc-applications/:id -> owner-only detail summary + profile.
POST /api/v1/ekyc-applications -> body {profile} -> detail, create status submitted atomically. Enforce one active submitted and prohibit duplicate if valid verified record exists. Retry conflicts409.
POST /api/v1/ekyc-applications/:id/withdraw -> {expectedVersion}, owner versioned submitted -> withdrawn.
GET /api/v1/ops/ekyc-applications?page&page_size&status -> paginated summaries (no decrypted profile in list).
GET /api/v1/ops/ekyc-applications/:id -> authorized reviewer detail + profile.
POST /api/v1/ops/ekyc-applications/:id/approve -> {expectedVersion,reviewNote,identityConfirmed:true}; reviewer cannot approve own application. Require nonblank <=1000 review note describing manual external evidence check. Writes approved application AND real existing EkycVerification in SAME transaction: provider operations_manual, unique ref manual-application-ID, status verified, actual verifiedAt, encrypted full verified name/kana, correct nameMatchHash normalization compatible ProtectedBankAccountService, resultHash, expiresAt null. This represents authorized manual checking, never auto-verifies self-report. Ensure current policy consumers will see it. Audit operator decision; no PII leak.
POST /api/v1/ops/ekyc-applications/:id/reject -> {expectedVersion,rejectionReason}; cannot review own; immutable terminal states; staleversion409; mandatory reason.
Permissions: ekyc-application:own (base applicants), ops:ekyc-application:read and :review (suitable operations roles). Dedicated permission migration must grant only same appropriate roles as existing owner and ops review permissions; idempotent inserts. Provide migration schema+permission operations, constants/seed/plumbing, app injection support for API tests, OpenAPI exact bodies/routes, localized error keys existing convention all current supported languages on frontend parent handles (report new keys). Migration un-applied can be authored, NEVER edit applied migration.
Implement controller/service/repository layers with tests for owner isolation, no self-review, concurrent submissions/version decisions, withdrawal/rejected resubmit, malformed profile, approval not triggered by submit, resulting verified record fields and audited actor. Existing identity docs may be updated backend section only via separate new docs/MANUAL_EKYC_WORKFLOW.md to avoid parent conflict.
Run targeted Jest and backend build/lint. Report full results to .superpowers/sdd/manual-ekyc-backend-report.md and send short summary, file list, API contract/new error keys. No fake/demo API, TODO, hardcoded ports/secrets; no disclosure of credentials.
