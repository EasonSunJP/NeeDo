# IM 快捷回复双槽位验收记录（2026-08-30）

## 范围

- 分支：`codex/im-reaction-slots-v2`
- 基线：`971fda50`
- 目标：同一用户、同一消息最多同时保留一个判断回复和一个普通表情；当前值可再次点击取消，同类别其他值不可选择。
- 目录：消息快捷回复与输入框表情面板统一为“常用表情 → 判断表情 → 一般表情”，折叠栏使用最近值并把“更多”固定在末尾。

## 自动化验证

- 前端功能聚焦测试：10 个文件、98 项通过。
- 后端功能聚焦测试：6 个测试套件、71 项通过。
- 前端全量测试：227 个文件、1366 项通过。
- 后端全量测试第一次运行：276 个套件通过、1 个无关的 `fee-rule-api` 用例出现瞬时 `ECONNRESET`；该文件随后单独复跑 3/3 通过。停止浏览器验收后端、释放额外数据库连接池后再次运行后端全量测试，277 个套件、1870 项全部通过（另有 9 个套件、37 项按既有配置跳过）；未修改费用规则模块代码。
- 前端 `npm run lint`、后端 `npm run lint`、后端 `npm run build` 通过。
- `npm run i18n:audit` 退出状态为 0。
- `npm run verify:production-build` 通过：正式 Vite 构建完成，production bundle audit 检查 8 个 HTML 入口和 22 个资源通过。
- i18n 分包实际大小为 3,700,913 bytes。为容纳本次新增的四语正式文案，i18n 预算从 3,700,000 精确增加 2 KiB 至 3,702,048；主包 4,000,000 bytes 预算未改变。

## 数据库与 migration

- 只读槽位检查结果：`{"ready":true,"duplicateSlotCount":0,"duplicates":[]}`。
- Prisma 状态显示最后共同 migration 为 `20260830060000_contact_delete_permission`。
- 仓库待应用：`20260830090000_message_reaction_slots`。
- 当前数据库存在仓库未包含的 migration：`20260830040000_formal_needo_exchange`。
- 因 migration history 存在分叉，本次未执行 `migrate deploy`，也未修改现有数据库历史。

## 5180 浏览器验收

- 使用隔离分支的正式前端构建运行在 `127.0.0.1:5180`，隔离分支正式后端运行在 `127.0.0.1:3000`；`/api/v1/health` 与 `/api/v1/ready` 均返回成功，MySQL 与 Redis ready。
- 已登录正式测试账号进入真实会话；消息操作栏展开顺序为“常用表情 → 判断表情 → 一般表情”。
- 判断目录完整显示 `OK`、`NO`、`Pending`、`+1`、`Done`、`Cool`、`Good`、`Thanks`，使用紧凑透明 SVG。
- 同一当前用户在同一消息上的 `OK` 与 `🥹` 同时显示为已选；刷新会话后仍读取为 `OK` 与 `🥹`，未出现第二个回复闪现后消失。
- 已选值保持彩色并可点击；判断槽位内其余 7 项和普通表情槽位内其余项均是原生 `disabled`。折叠栏中已选 `🥹` 可点击，其他同类最近 emoji 不可点击。
- 输入框表情面板使用同一目录。选择 `Thanks` 写入未发送草稿后，消息折叠栏立即把 `Thanks` 显示为最近第一项；因目标消息已有 `OK`，该项在消息菜单中正确 disabled。测试草稿已清空。
- 390 × 844 视口下 `documentScrollWidth === documentClientWidth === 390`；输入框面板与消息操作菜单没有横向溢出，消息操作菜单边界为 16–374px。
- 浏览器仅完成读态、草稿目录与刷新持久化验收；对正式测试消息执行取消/重新添加属于会改变他人可见状态的操作，等待明确授权后再执行。精确取消、幂等 PUT、同类别冲突以及两个用户隔离已由后端 API/Repository 测试覆盖。

## 未执行事项

- 未应用数据库 migration。
- 未合并回 `main`。
- 未 push、未创建 PR、未部署。
