# 技师状态与考勤：最新 main 集成验收

2026-09-06，独立集成分支 `codex/technician-work-status-main`，基于已包含正式通知与 SOS 的 main `cf9f7558`。仅重放 `171bc017`、`6d63bddb`、`804eb41d` 的功能差异；未合并 `22a2e32b` 快照或主目录其他未提交改动。

## 集成结果

- 保留 main 的 API 命名空间隔离、OfficialNoticeBell、SOS 及用户 LOG。新增工作状态路由位于隔离拦截之后，运营/商户的读取、事件、补充记录端点均有跨端 404 回归。
- 保留 Booking 新增的平台支付策略参数，状态通知接在其后；开始、结束和自动到期事件仍沿原正式事务提交。
- 管理端工作时间线改用当前 AdminEventTimeline，继承主题、三列布局和超过十行展开；技师端使用移动时间线。先补失败渲染测试，再接入统一组件。
- 保留用户与员工列表胶囊详情和右侧冻结列，保留现有用户 LOG/订单动态分页。
- 状态保存、排班/预约零宽限迟到早退、不可变事件、月度次数、七种东京日期范围与类型筛选均沿正式数据库/API，不引入模拟数据。

## 新鲜验证

| 检查 | 结果 |
| --- | --- |
| 功能与时间线前端回归 | 11 文件，74 tests 通过 |
| 列表与共享时间线额外回归 | 4 文件，26 tests 通过；与上项存在组件覆盖重叠 |
| 后端状态/订单/资料/API隔离/UTC migration | 8 suites，86 tests 通过 |
| 前端正式构建、后端构建 | 通过 |
| 生产产物审计 | 8 HTML entries / 55 assets 通过 |
| 后端差异 ESLint | 30 TypeScript 文件通过 |
| Prisma Client 与差异检查 | 私有客户端生成通过，git diff --check 通过 |
| 共享 MySQL 正式事务检查 | 通过并回滚夹具 |
| 独立 MySQL 8 新安装 | 完整 145 条 migration 通过，正式事务检查通过并回滚 |
| 独立集成代码审查 | 无 Critical / Important 阻断项 |

独立 MySQL 8 使用 +09:00 会话时区。最后迁移后的 epoch 为 `2026-09-06 11:09:09.346`，查询 UTC 时间为 `11:09:45.271`，确认启用边界没有九小时延后。共享库之前已应用本功能三条 migration；本轮没有重写历史、重新执行共享迁移或改变全局时区。

正式回滚检查覆盖零宽限、个人可预约排班排除、工作器去重、补到岗结清、CAS、幂等重放、早退确认预览、稳定次数、东京自然月、分页、半开日期区间、商户隔离、评论及审计。

## 实际三端浏览器

验收服务为本分支的 15184 前端、13340 client API、13341 ops API 和 13342 merchant API。四个监听 cwd 已确认；最后三 API 的 /api/v1/ready 均返回 200，MySQL/Redis 正常。使用正式测试账号通过真实登录 UI；技师为 profile 186，没有注入 token、假权限或假接口。

- 出勤约 0.54 秒、休息约 0.32 秒反映在三端正式状态中。
- 重新登录及刷新后仍为休息，技师和运营时间线显示持久化的“出勤 → 休息”，商户员工动态显示同一记录。
- 本月卡片为真实的 0 / 0；未为截图修改预约或制造异常。非零异常界面有前次独立环境验收，本轮异常边界再次由真实数据库事务检查覆盖。
- 月度卡片打开明细，七种日期范围、全部/迟到/早退筛选通过；自定义 2020-01-01 至 2020-01-02 返回 0 条。
- 技师、商户和运营 390px 页面均无横向溢出。截图前等待启动遮罩结束。

截图：[技师窄屏](final-technician-narrow.png)、[运营时间线](final-operations-timeline.png)、[商户时间线](final-merchant-admin.png)、[运营指标](final-admin.png)、[日期类型筛选](final-filters.png)、[运营窄屏](final-admin-narrow.png)。

## 集成与运行边界

本记录的浏览器证据来自上述独立正式三 API 环境，不将它描述为共享 5180 的验收。用户已授权通过验证后本地合入 main；未推送远端或部署。合并后的提交状态另由 Git 记录确认。

验收结束后停止本任务的 13340 状态工作器，避免与 SOS 任务清理其专用临时技师发生重复扫描；SOS 任务只处理其自身夹具。最终 profile 186 保持休息。临时 SQL/环境配置不纳入提交；回滚代码仅撤销本功能提交，保留正式事件与审计数据，不执行破坏性降级。
