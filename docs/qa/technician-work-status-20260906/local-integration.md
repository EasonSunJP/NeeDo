# 技师状态同步：主工作区集成验收

2026-09-06。本记录补充并更新 [独立分支验收](acceptance.md) 的集成边界；此前的隔离环境截图和迁移阻塞属于历史结果。

## 集成与数据库

- 功能提交 `171bc017`、UTC 修正提交 `6d63bddb` 的差异已经应用到 `/Users/eason/Documents/New project` 的工作文件。保留原分支 `codex/portal-session-isolation`、原有暂存区及其他任务的修改，没有将含其他任务改动的快照基线整体合并，也没有提交整个脏工作区。
- 原 SOS migration 的 CHECK/FK 问题已由其他任务在主工作区修正。本次保留该修正，完整空库迁移链实际通过：先应用 123 条，再应用 UTC 修正，合计 124 条。
- 本地共享 MySQL `127.0.0.1:3307/needo_dev` 成功应用三条新增 migration：`20260906190000_technician_work_status`、`20260906200000_work_status_affected_orders`、`20260906210000_work_status_epoch_utc_bootstrap`。
- 本地 MySQL 继承日本时区。新增第三条 migration 使用 `UTC_TIMESTAMP(3)` 修正尚无任何状态、事件或异常历史的新启用时间；已有历史（含软删除历史）时不移动启用边界。已应用 migration 未改写，共享数据库全局时区未调整。迁移后检查启用时间与 UTC 当前时间一致，未出现九小时延后。
- 共享库中存在早于本任务、在当前分支找不到文件的已应用 migration。未更改这些历史记录；本次成功应用自己的三条增量，不将结果描述为整个共享库历史完全一致。
- 迁移前完整 SQL 备份保存在本机 `.superpowers/work-status-integration/needo_dev-before.sql`，约 111.6 MB、权限 600；原工作文件备份位于 `/tmp/needo-work-status-root-before-20260906`。备份与私密配置均不纳入提交。

## 主工作区验证

下列检查均实际通过，未声称全仓库测试通过：

| 验证 | 结果 |
| --- | --- |
| 前端定向回归 | 9 文件 / 67 tests |
| 后端状态、资料 API、订单履约与到期回归 | 7 suites / 74 tests |
| UTC migration 条件回归 | 1 suite / 11 tests |
| 真实 MySQL UTC 修正与历史保护 | 9 项，事务回滚，无夹具残留 |
| 共享数据库 `check:work-status-flow` | 通过，使用事务夹具并回滚 |
| 前端 production build / 后端 TypeScript build | 通过；前端仍有既有包体积提示 |
| 后端本任务 29 个 TypeScript 文件 ESLint | 通过 |
| 本任务差异空白检查 / Prisma generate | 通过 |

最终前端构建命令显式清空 `VITE_LEGACY_AUTHORIZATION` 与 `VITE_LEGACY_AUTH_BASE_URL`，不携带本地旧认证配置；保留生产安全检查，不修改本地配置文件。

真实数据库流程覆盖零宽限、排除个人可预约时间、工作器去重、迟到结清、CAS、命令重放、早退确认预览、稳定次数、东京自然月、分页、半开日期区间、商户隔离、运营身份别名、评论、审计和回滚。

## 实际三端浏览器

用真实登录页面登录 `admin@lifedance.com`，以及截图中的 `admina@lifedance.com` 的技师和商户身份。没有注入虚假权限或模拟 API。

- 主工作区前端运行于 `http://127.0.0.1:15180`，正式后端端口 13318，连接上述共享本地 MySQL 及 Redis 6379。CORS 来源仅为此次本地验收显式配置。
- 技师“出勤”提交成功，商户员工列表与运营详情约 1.2 秒内显示出勤；随后“休息”同样在三端同步。
- 技师重新载入后仍为休息，三端时间线均显示实际 `出勤 → 休息` 事件。最终该测试技师保持休息。
- 运营“业务状态与正式指标”的迟到/早退卡片可打开明细，支持七种日期范围及全部/迟到/早退筛选；自定义 `2020-01-01` 至 `2020-01-02` 配合早退返回空列表。
- 月度卡片以“本月”及“迟到数 / 早退数”紧凑显示，保留完整的无障碍说明，避免窄列拉高整排指标；调整后定向 2 项测试通过。
- 当前测试账号本月迟到/早退均为 0，不为截图伪造异常或修改排班、预约。异常判定及非零次数的界面验收见之前独立环境记录，正式数据库边界由上述事务流程检查覆盖。
- 技师 390px 页面无横向溢出。时间线使用日期、节点连线、头像/系统图标和主题色气泡，保持正式补充记录入口。

截图：[运营时间线](integrated-operations.png)、[商户时间线](integrated-merchant.png)、[技师窄屏](integrated-technician.png)、[月度指标](integrated-metrics.png)、[筛选明细](integrated-filters.png)。

## 当前运行与回滚边界

验收期间，另一任务将共享 5180/3000 切换到了 `.worktrees/dashboard-remaining-acceptance`。本任务没有覆盖对方进程，因此最终浏览器结果来自已核实 cwd 的主工作区 15180/13318，不能宣称 5180 当前展示本功能。代码已集成到主工作区工作文件，独立分支提交保留；尚未合并到 main、推送远端或部署线上。

回滚代码时仅撤销本任务差异，保留其他任务改动。新增表中的正式状态和审计应保留，不执行破坏性降级；SQL 全库备份仅用于需要时的独立恢复，不能直接覆盖并行任务正在使用的共享库。
