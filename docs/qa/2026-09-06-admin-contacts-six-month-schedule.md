# 两个管理员账号的联系人与六个月排班

## 授权与范围
用户要求先写本地，全部完成后同步新版本和数据到 staging。窗口为日本时间 2026-09-06 00:00 至 2027-03-06 00:00（结束不含），共 181 天。按每家店所有时刻至少 10 人在岗执行，50 名测试技师轮换，三班制，每班 8 小时、10 人，每日 30 个不同人员，相邻新班次至少休息 16 小时。两店使用互不重叠的人员池。

## 已写入本地并经正式 API / 数据库核对
每个账号的 customer、technician、merchant_owner 身份分别建立联系人；只新增启用测试账号的有效身份，保留原联系人。

| 账号 | 身份 | 用户联系人 | 技师联系人 | 商户联系人 |
| --- | --- | ---: | ---: | ---: |
| admin@lifedance.com | 用户 | 22 | 50 | 5 |
| admin@lifedance.com | 技师 | 20 | 50 | 5 |
| admin@lifedance.com | 商户 | 30 | 50 | 5 |
| admina@lifedance.com | 用户 | 21 | 50 | 5 |
| admina@lifedance.com | 技师 | 20 | 50 | 5 |
| admina@lifedance.com | 商户 | 20 | 50 | 5 |

| 店铺 | 新轮班技师 | 当前名单/头像 | 新在岗区间 | 新预约时段 |
| --- | ---: | ---: | ---: | ---: |
| LifeDance Wellness 渋谷（本地 16） | 50 | 50 / 50 | 5430 | 41223 |
| 麻布十番超级按摩（本地 217） | 50 | 51 / 51 | 5430 | 43440 |

已核对每店 4344 个小时均有至少 10 个不同技师在岗。第一家店保留原有 45/90 分钟预约与时段，因此部分小时不足 10 个完整的新增 60 分钟可用预约位；人员在岗覆盖与空闲可预约容量是不同指标。原订单、财务与密码不改动。

数据命名空间：admin_6m_20260906_v1。规范计划按邮箱映射，不使用本地 ID 作为 staging 导入依据。计划摘要：e02c3311367af4e5784927bbc1f67f6fad4f9601047d6057ec3b76ac7d55d152。按联系人、店铺关系、每日班次分别事务和审计。首次写入后重复执行确认未增加每日记录。

## 日程修复
- 详细排班复用统一日历，接正式分页排班 API、真实店铺技师及头像，不再固定在 2026-04-20。
- 店铺详情聚合有效合作技师，去重并保留真实头像，解决仅返回主店技师导致合作人员头像缺失。
- 跨午夜记录按日本时间逐日裁剪，午夜结束显示前日 24:00，周期起点也显示前夜延续班次。
- 补齐加载、失败、重试与正式空排班的展示。

## 复核与剩余事项
1. 用户在 2026-09-06 明确批准 53 条测试签约调整后已执行：admin 组 20 条、admina 组 33 条，审计 ID 9748、9749。原店从属关系与订单保留，关系类型改为 PARTNER。完整本地验收命令再次通过，其他店独占冲突为 0，六个身份联系人与两店各 4344 个小时覆盖均通过。
2. AWS 既有 staging 资源在账号 430611185505，但用户授权码返回账号 358331615160 的 IAM 用户。没有覆盖旧 profile，没有向新账号部署。等待正确账号登录或用户提供迁移后的目标说明。
3. 本地隔离版本已完成两账号正常密码登录和移动端日程检查，详见下方续验记录。最新 main 集成后的最终浏览器验收、staging 数据与浏览器验收尚未执行。

## 验证与隔离
主工作区存在其他任务改动；5180 由 membership-benefit-catalog 工作区提供。此任务最初使用 5289 作为主工作区本地页面入口，后切换为隔离工作区的正式构建预览（PID 89502），避免其他开发任务的热更新；发布源隔离在 .worktrees/admin-six-month-release（codex/admin-six-month-release），只带本任务及所需排班接入改动。

前端 4 文件 29 项测试通过；初始主工作区 lint 和 formal 构建通过。后端联系人/排班计划及店铺读取 17 项测试通过（初始主工作区），隔离工作区重新验证完成：前端 29 项、后端 16 项、发布工具 16 项，共 61 项专项测试通过；前端 formal 构建、8 HTML/35 资产审计、后端完整构建、脚本严格类型检查与变更后端 ESLint 均通过。维护脚本已编译成可在生产依赖环境执行的 CommonJS 产物，并在本地数据库执行只读核验通过。普通 production 构建被本机旧认证环境配置守卫拒绝，formal 模式构建用于正式验证。独立审查发现并已修复 ReadCommitted、锁后幂等检查和回滚前锁定技师/下游检查等并发问题。

## 运维命令
在 backend 下使用 ENV_FILE 指定正式环境文件，不在命令或文档中提供密钥：
- 只读计划：node --import tsx scripts/provision-admin-six-month.ts --export=<private-plan>
- 读取既有计划并应用：node --import tsx scripts/provision-admin-six-month.ts --plan=<private-plan> --apply-contacts --apply-schedules
- 仅在用户明确批准签约调整后加 --prepare-partners。
- staging 必须追加 --target=staging，且 DEPLOY_ENV=staging、目标数据库名称包含 staging。
- 精确回滚：同一计划加 --rollback；如生成时段有订单/Exchange 业务记录则拒绝回滚。
- 验证：ADMIN_STAFFING_PLAN=<private-plan> FORMAL_API_URL=<formal-api> node --import tsx scripts/check-admin-six-month.cjs

staging 后续需实时预检版本、备份受影响数据、验证服务与账号映射，完成版本部署和规范计划导入，再核对六个身份、两家店逐小时覆盖及实际头像显示。当前没有 staging 发布完成证据。2026-09-06 继续执行时，AWS 会话仍过期；新登录流程停在显示旧账号 358331615160 的页面，自动审批审核拒绝点击“登录新会话”，等待用户手动切换正确账号。

## 2026-09-06 本地浏览器续验

- 通过普通登录表单进入两家真实测试店铺，不注入 token、不改密码；保存密码开关关闭。
- 440×956 移动端：admin 的预约一览和详细排班各 50 个技师头像；admina 的两个入口各 51 个头像，两账号分别逐一比对姓名与图片地址一致。已排筛选保留正式日程。
- 浏览器发现预约一览在 appointmentStatus 模式禁用了技师列，且只读取首 100 条。已复用完整分页加载器，传入正式店铺技师，保留按技师列和已排/未排筛选；正式页面停用本地新增行程。
- 复审补齐已离职或不在公开名单的历史技师列，防止历史日程消失。跨午夜时段逐日拆分，午夜结束为 24:00，无次日空事件。
- 新增四项回归均先失败后修复。五个相关测试文件共 50 项通过；lint、formal 构建和 8 HTML/35 资产审计通过，新增逻辑二次只读复审通过。
- 验收运行来源：5289 预览 PID 7083，3019 后端 PID 9966，均来自 admin-six-month-release 工作区；11:00 UTC 延迟复查代理 ready 为成功，MySQL/Redis 均正常。
- 旧基线的退出动作仍会短暂返回店铺页面，刷新才回登录。按当前协调要求等待 SOS/work-status 合并后，语义重放到最新 main 的隔离集成分支，重新验收身份切换及两账号页面，不在旧基线扩大认证修改。
- 本轮没有修改联系人、排班或53条签约关系，没有推送/部署。53条关系的明确授权原文是用户对对应审批问题回复“明确批准这 53 条签约调整”；审计 9748/9749 属于此前已执行记录，不是本轮新增。
- 57e7e099 的旧发布归档未包含本轮前端修复，不可作为最终发布包；最新基线集成及最终验收完成后重新打包。

## 真实预约与可预约排班的区分

2026-09-06 用户再次要求所有显示都必须来自真实测试账号、正式数据库。截图中前三名技师的零点卡片对应 schedule_slots 86302、86310、86318，均关联启用的测试技师账号，AVAILABLE，bookedCount=0，关联 BookingOrder 为空。这些是真实落库的可预约班次，并非已经有客户下单。

当天正式 /orders 与数据库 ID 集合核对相同：admin 店共21条（12 confirmed、6 pending、3 cancelled），全部测试客户；admina 店当天0条。不能把可约班次当作已有预约。

因此预约一览改为读取按当前商户身份授权的正式订单列表，排除取消订单、保留真实订单ID和订单详情入口；详细排班继续显示正式时段，并在紧凑卡片保留“可预约/有预约/已锁定”等真实状态。已分配技师不再作为存在客户预约的证据。

订单查询新增可选 dateMode=overlaps，要求完整且不超过93天的窗口，使用 startsAt < to && endsAt > from；省略时保持原有按开始时间过滤。所有身份、状态及软删除过滤保持原样。跨午夜事件逐日拆分。新增前后端回归先失败后通过：前端7文件69项、后端3文件28项；前后端构建、变更后端ESLint、8 HTML/35资产审计通过，静态复审通过。

旧基线上的新订单页面和重启后的正式重叠查询尚未完成最终浏览器/数据库验收：旧后端重启后测试登录返回token_invalid。前置main已就绪，下一步在最新main隔离工作树重放本任务，使用当前认证契约重新验证，不修改密码或会话权限来规避。

## 最新 main 语义集成检查点（2026-09-06 20:53 JST）

隔离分支 `codex/admin-six-month-main-integration` 起于 `b3173885`；只重放本任务，不携带旧 staging 工具或旧认证实现。后续同步协调主线 `fd03470b`，不自行合并 main、不推送、不部署。

- 保留主线 `technicians` 参数、评价校验与当前认证实现。前端10文件88项、后端5文件55项、新增首页/设置3文件46项通过；正式构建、8 HTML/55资产审计、相关后端ESLint通过。
- 两账号正式订单API与数据库ID集合完全一致；重叠窗口包含已经开始但仍未结束的订单28293，原startsWithin模式不包含它。admin当天18条未取消，admina为0。普通浏览器登录后计数相同；admin真实订单LDF26-0000827可从日程打开详情。
- 两账号各六个月4344小时在岗覆盖及六身份联系人重新只读检查通过。浏览器admin两入口50头像、admina两入口51头像，分别姓名和图片地址完全一致；手机440×956排班显示真实“可预约”状态。
- 修复正式详细表切店/跨周期时旧时段遗留及加载失败回退legacy，始终保留正式空数据覆盖和加载/错误提示。统计“确定订单”原为有预约的时段数，已改成准确的“有预约时段”。
- 修复沿途发现的正式订单详情假粉丝/KYC/等级/预计到达提示，金额不再取未收款的0；修复首页固定流水/利用率及空legacy订单，改读正式today经营接口及完整订单；退出导航直接回对应登录页。这些补充修复的最终浏览器检查仍在进行。
- 本地admin登录Redis会话版本再次落后（DB=3/Redis=0），通过既有RedisAuthSessionStore撤销旧refresh并恢复DB权威版本，审计10455/10456；未改DB会话版本、密码或角色权限。暂未确认哪个并行流程回写了旧Redis版本，不能声称已解决其产生源头。
- 53条关系按审计9748/9749的精确ID只读检查：53条PARTNER、0删除、0结束。可核验授权来自本任务用户对明确审批问题的回复原文“明确批准这 53 条签约调整”。本轮未新增、改写或回滚这些关系。
