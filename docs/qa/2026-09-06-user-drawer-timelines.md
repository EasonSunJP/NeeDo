# 用户抽屉时间线统一（Step 12 微步骤）

用户给定图 3 为布局依据，沿用运营/商户当前主题：左侧日期时间、连续圆节点线、头像/系统图标、圆角内容气泡。弱化标题分隔和元数据边框。审计只读；履约评论使用已有权限及正式追加接口。

- 先补审计分页、参数校验与履约评论交互失败测试。
- 用户详情接口增加 audit_page / audit_page_size（10、50），返回审计分页元数据，保留范围/RBAC/审计。
- 用户动态顶部显示分页；切用户/条数回第一页，失败可重试，忽略过期响应。
- 两处使用共享时间线，后台主题局部映射。
- 执行前后端专项测试、lint/build、桌面与窄屏浏览器检查。

修改位置：unified-user-management-revision（现有 5174 运行目录）。无 migration、无部署。

## 实现与验收

- 用户动态和履约流程复用 `AdminEventTimeline` / `ContactEventTimelinePanel`，日期时间在左、节点连续相连、真实操作者头像与圆角气泡在右。正常节点使用当前后台强调色，取消/异常使用危险色。
- 用户动态标题不再有醒目的白色分隔线；元数据去掉白色描边，分页背景沿用后台 surface/line token。
- 运营和商户用户详情读取真实审计分页，默认 10 条，可选 50 条，数据库执行 skip/take；参数不合法返回 400，原有范围过滤、RBAC 与审计保留。
- 翻页失败保留当前记录并提供重试；切换条数失败后再翻页，使用当前实际显示的条数，回归测试已先失败后通过。
- 履约评论入口沿用图 3 底部节点形式，打开原有正式评论表单，提交仍调用既有追加 API。浏览器仅验证展开，不写入测试评论。

验证结果（2026-09-06）：

- 前端专项：22 个测试文件、103 项通过；`npm run lint`、`npm run build` 通过。构建仅有现有动态导入和大包提示。
- 后端用户列表/履约 repository、API、OpenAPI 六组专项通过；另补充的真实头像/商户评论范围测试通过。后端 lint/build 通过。
- 已登录 Chrome：119 条真实审计，10 条时 1/12 → 2/12；50 条时 1/3 → 2/3，DOM 实际渲染 50 条。桌面及 390×844 窄屏检查完成，抽屉无横向溢出。
- 履约单 `ND202609032156246975` 的三条正式事件显示系统节点、操作者头像、取消节点及原因；底部评论入口可打开表单。
- 最终复查：5174 前端 PID 79786，3003/3004/3005 后端 PID 74744/74746/74745，cwd 均为该工作树；运营 `/api/v1/ready` 返回 database/redis 均 ok。

交付边界：本地工作树修改；未合并 main、未推送、未部署。未执行全仓库测试。回滚可撤销本次独立提交，无数据库 migration。
